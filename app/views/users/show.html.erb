<% provide(:title, "PillBox | #{@user.name}") %>
<h1><%= @user.name %></h1>
<div class="row">
	<div class="span6 offset3">
		<% if @user.dailies.any? %>
			<%= render 'shared/daily_report' %>
		<% else %>
			<p>
				No reports found.
			</p>
		<% end %>
	</div>
</div>