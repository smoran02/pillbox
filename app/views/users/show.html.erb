<% provide(:title, "PillBox | #{@user.name}") %>
<h1><%= @user.name %></h1>
<div class="row">
	<div class="span6 offset3">
		<p>You have <%= @user.dailies.count %> reports.<br />
		View your most recent report <%= link_to "here",	root_url %>.</p>
	</div>
</div>