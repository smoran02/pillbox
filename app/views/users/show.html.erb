<% provide(:title, "PillBox | #{@user.name}") %>
<h1><%= @user.name %></h1>
<div class="row">
	<div class="span6 offset3">
		<p><%= @user.name %> has <%= @user.dailies.count %> reports.<br />
		View the most recent daily <%= link_to "here",	root_url %>.</p>
	</div>
</div>